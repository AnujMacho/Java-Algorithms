Simple Array Sum

static int simpleArraySum(int[] ar) {
        /*
         * Write your code here.
         */
         int count = 0;
         for(int i=0;i<ar.length;i++){
             count += ar[i];
         }
         return count;
}
