static void miniMaxSum(int[] arr) {
        long maxVal = 0;
        long minVal = 0;
        Arrays.sort(arr);
        for(int i=0;i<arr.length;i++){
            if(i<arr.length-1) minVal +=arr[i];
            if(i>0) maxVal+=arr[i];
        }
        System.out.println(minVal+" "+maxVal);
    }
